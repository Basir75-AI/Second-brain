# wiki/queries/

Filed answers to questions worth keeping — comparisons, gap analyses, synthesis across several pages. Keeps exploration compounding instead of evaporating into chat history.

Naming: `q-<slug>.md`. Skeleton: `wiki/_meta/entity-skeleton.md`.
