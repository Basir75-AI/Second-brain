# wiki/clients/

One page per client account: engagement shape, contacts, history, and which service lines they buy. Facts here must be sourced — never guess a client detail.

Naming: `<client-name>.md`. Skeleton: `wiki/_meta/entity-skeleton.md`.
