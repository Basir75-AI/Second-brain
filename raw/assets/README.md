# raw/assets/

Images, PDFs, and exports referenced by sources. Point Obsidian Web Clipper here
(Settings -> Files and links -> Attachment folder path) so clipped images land locally
rather than staying as remote URLs that can break.
